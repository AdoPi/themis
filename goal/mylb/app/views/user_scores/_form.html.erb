<%= form_with(model: user_score, local: true) do |form| %>
  <% if user_score.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_score.errors.count, "error") %> prohibited this user_score from being saved:</h2>

      <ul>
      <% user_score.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_name %>
    <%= form.text_field :user_name %>
  </div>

  <div class="field">
    <%= form.label :points %>
    <%= form.number_field :points %>
  </div>

  <div class="field">
    <%= form.label :leaderboard_id %>
    <%= form.text_field :leaderboard_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
